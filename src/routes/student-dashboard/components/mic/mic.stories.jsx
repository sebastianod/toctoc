import React from "react";
import Mic from "./mic.component";

export default {
    title: "test/Mic", //must be a unique name
    component: Mic, //the actual component
};

export const MicComponent = () => <Mic />; //MicComponent is the name of the story